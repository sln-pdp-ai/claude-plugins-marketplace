# Clones de travail que les scripts des plugins peuvent provisionner lors d'un test local
.smt-tmp/

# Réglages Claude Code propres à chaque utilisateur (approbations MCP, permissions locales)
.claude/settings.local.json
