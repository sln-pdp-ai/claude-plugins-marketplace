# Checkouts locaux en lecture seule des dépôts SMT (non suivis ici)
repos/

# Clones de travail provisionnés par les plugins SMT (voir plugins/*/README.md)
.smt-tmp/

# Réglages Claude Code propres à chaque utilisateur (approbations MCP, permissions locales)
.claude/settings.local.json
